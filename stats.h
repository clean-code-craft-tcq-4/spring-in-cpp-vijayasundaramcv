#include <vector>

typedef auto Stats;
typedef struct computedStats
{
    float average;
    float min;
    float max;
}computedStats;

namespace Statistics {

    Stats ComputeStatistics(const std::vector<___>& );
}
